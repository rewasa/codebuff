{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "composite": true,
    "module": "esnext",        // Bun likes modern modules; Node 22 handles them too
    "types": ["bun", "node"]
  },
  "include": ["*.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts",
    "__tests__"
  ]
}
